## How to Compile

This program consists of multiple C files (`main.c` and `game.c`), so both files need to be compiled together.

Using GCC:

```bash
gcc main.c game.c -o game
```

Then run the program:

```bash
./game
```

On Windows:

```powershell
.\game.exe
```
